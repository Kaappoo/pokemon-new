node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
*.env
*.exe
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json
backend/backend

# This project standardizes on pnpm (pnpm-lock.yaml) - don't let npm/yarn
# regenerate their own lockfiles, which silently drift from it.
package-lock.json
yarn.lock
